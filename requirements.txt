SQLAlchemy>=2.0.0

psycopg2-binary>=2.9.0

# Data validation (DTO)
pydantic>=2.0.0

# Optional (recommended if using FastAPI later)
fastapi>=0.110.0
uvicorn>=0.29.0

# Optional (for migrations - very recommended)
alembic>=1.13.0